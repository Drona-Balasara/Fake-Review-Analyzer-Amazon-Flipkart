# Netlify redirects file
# Handle API calls by redirecting to mock data or client-side processing

# API endpoints redirect to client-side handling
/api/analyze    /index.html    200
/api/history    /history.html  200
/api/delete     /index.html    200
/auth/*         /index.html    200

# Handle PHP files by redirecting to HTML equivalents
/analyze.php    /index.html    200
/history.php    /history.html  200

# Only redirect HTML pages that don't exist, not static assets
# This allows CSS, JS, images, and other static files to load normally